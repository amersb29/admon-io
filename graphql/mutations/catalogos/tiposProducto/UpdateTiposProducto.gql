mutation(
    $id: ID!
    $description: String!
    $price: Float!
){
    res: updateTipoProducto(id: $id, description: $description, price: $price){
        id
        description
        price
    }
}